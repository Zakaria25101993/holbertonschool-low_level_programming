#include "main.h"
/**
 * mul - Checks if a character is a digit between 0 and 9.
 * @a: The character to be checked.
 * @b: The character to be checked.
 * Return: produit de a * b
 */

int mul(int a, int b)
{
return (a * b);
}
